export type Config = {
  chromeExecutablePath: string;
  scrapboxProject: string;
};

const config: Config = {
  chromeExecutablePath:
    "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome",
  scrapboxProject: "xxxxxxx",
};

export default config;
